# Copy to .env and fill in. Gitignored.

# Only needed for the asset prefetch. Qwen2.5 and cais/mmlu are ungated, so this
# can usually stay empty.
HF_TOKEN=

# Point at shared storage so compute nodes can read the snapshots offline.
HF_HOME=
